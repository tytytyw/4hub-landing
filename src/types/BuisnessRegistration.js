import PropTypes from "prop-types";

export const compareProps = PropTypes.exact({
  isAgreed: PropTypes.bool,
  isCoincidePass: PropTypes.bool,
  isLogin: PropTypes.bool,
  isPass: PropTypes.bool,
  isСompany: PropTypes.bool,
});
